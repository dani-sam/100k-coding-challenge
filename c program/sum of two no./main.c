#include <stdio.h>
#include <stdlib.h>

int main()
{
int num1,num2,sum;
    printf("Enter two no:");
    scanf("%d%d",&num1,&num2);
    sum=num1+num2;
    printf("the result is %d",sum);
    return 0;
}
